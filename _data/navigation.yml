# The following is the order of the links in the header of the website.
#
# For a simple single-page academic site like nardien.github.io,
# we keep the navigation minimal.

main:
  # Minimal navigation - most content is on the main About page
